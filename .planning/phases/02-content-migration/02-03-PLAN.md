---
phase: 02-content-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/content/blog/**
  - src/content/pages/**
  - src/content/videos/**
  - scripts/migrate.mjs
autonomous: false
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - PERF-01

must_haves:
  truths:
    - "A human has reviewed sample migrated content and confirmed quality is acceptable"
    - "All 66 blog posts have clean, readable markdown without WordPress artifacts"
    - "All video pages have correct YouTube IDs or WordPress.tv URLs"
    - "Featured images render correctly in frontmatter with relative paths"
  artifacts:
    - path: "src/content/blog/"
      provides: "66 verified blog post directories"
    - path: "src/content/pages/"
      provides: "Verified page markdown files"
    - path: "src/content/videos/"
      provides: "8 verified video markdown files"
  key_links:
    - from: "src/content/blog/*/index.md"
      to: "src/content/blog/*/images/"
      via: "all image references resolve to existing local files"
      pattern: "\\./images/"
---

<objective>
Human review of migrated content quality and final fixes.

Purpose: Content migration is the highest-risk phase in this project. A human eye-check on sample content ensures nothing was mangled by the automated migration. This is also the final gate before Phase 3 builds routing on top of this content.

Output: Verified, human-approved content ready for Phase 3 routing.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-migration/02-CONTEXT.md
@.planning/phases/02-content-migration/02-01-SUMMARY.md
@.planning/phases/02-content-migration/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate content migration report</name>
  <files>scripts/migrate.mjs</files>
  <action>
Create a quick audit of the migrated content by running these checks and compiling a report:

1. **File counts:** Count blog posts, pages, videos, total images
2. **Frontmatter completeness:** For each blog post, check that title, publishDate, and categories exist. Flag any with missing required fields.
3. **Image integrity:** For each blog post with a featureImage in frontmatter, verify the referenced image file exists on disk. Flag any broken references.
4. **WordPress remnant scan:**
   - `grep -r "joost.blog/wp-content" src/content/` (should be zero)
   - `grep -r "<!-- wp:" src/content/` (should be zero)
   - `grep -r "Code language:" src/content/` (should be zero)
   - `grep -r "&amp;" src/content/` (should be zero or minimal)
   - `grep -r "Estimated reading time:" src/content/` (should be zero)
5. **Video metadata:** List all 8 videos with their youtubeId or videoUrl values to confirm correct assignment
6. **Sample content preview:** Show the first 30 lines of 3 blog posts (one recent, one old, one with lots of images like traveling-to-peru) so the human can visually verify quality

Print all results to stdout as a structured report.
  </action>
  <verify>
    <automated>echo "Blog posts:" && ls src/content/blog/ | wc -l && echo "Pages:" && ls src/content/pages/*.md 2>/dev/null | wc -l && echo "Videos:" && ls src/content/videos/*.md 2>/dev/null | wc -l</automated>
  </verify>
  <done>Migration report generated showing file counts, completeness checks, remnant scan results, and sample content previews.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of migrated content quality</name>
  <files>src/content/blog/**, src/content/pages/**, src/content/videos/**</files>
  <action>
Present the migration report to the user for review. All WordPress content has been migrated to Astro content collections:
- 66 blog posts in src/content/blog/{slug}/index.md with co-located images
- ~10 pages in src/content/pages/{slug}.md
- 8 videos in src/content/videos/{slug}.md with YouTube/WordPress.tv metadata
- ~166 images downloaded locally

The user should:
1. Review the migration report output for any warnings or issues
2. Open a few blog posts in their editor to check formatting:
   - `src/content/blog/tailwind-paradox/index.md` (recent, has featured image)
   - `src/content/blog/traveling-to-peru/index.md` (has many inline images)
   - `src/content/blog/stepping-down-at-yoast/index.md` (personal/important post)
3. Check a video file: `src/content/videos/wordpress-seo-in-2019.md` (should have youtubeId: pS-OhpUMC10)
4. Check a page: `src/content/pages/about-me.md` (should have clean content, no WordPress artifacts)
5. Verify images exist: `ls src/content/blog/tailwind-paradox/images/`
6. Run `npx astro check` to confirm schema validation passes
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -5</automated>
  </verify>
  <done>Human has approved content migration quality. Content is ready for Phase 3 routing.</done>
</task>

</tasks>

<verification>
- Human has reviewed sample migrated content
- All automated checks pass (file counts, no WordPress remnants, schema validation)
- Content is ready for Phase 3 routing
</verification>

<success_criteria>
- Human approval of content migration quality
- All 66 blog posts, ~10 pages, 8 videos validated and present
- Zero WordPress artifacts remaining in content files
- All image references resolve to existing local files
- Astro schema validation passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-migration/02-03-SUMMARY.md`
</output>
