---
phase: 03-routing-and-core-pages
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/pages/category/[slug].astro
  - src/pages/videos/index.astro
  - src/pages/videos/[slug].astro
  - src/pages/404.astro
  - src/components/YouTubeEmbed.astro
autonomous: true
requirements:
  - BLOG-03
  - BLOG-07
  - URL-05

must_haves:
  truths:
    - "Each category has an archive page at /category/{slug}/ showing all posts in that category"
    - "Videos hub at /videos/ lists all 8 videos auto-generated from the collection"
    - "Individual video pages at /videos/{slug}/ show YouTube embed and description"
    - "A custom 404 page displays when visiting a non-existent URL"
  artifacts:
    - path: "src/pages/category/[slug].astro"
      provides: "Category archive pages for all categories"
      exports: ["getStaticPaths"]
    - path: "src/pages/videos/index.astro"
      provides: "Auto-generated videos hub page"
      min_lines: 20
    - path: "src/pages/videos/[slug].astro"
      provides: "Individual video pages with YouTube embed"
      exports: ["getStaticPaths"]
    - path: "src/pages/404.astro"
      provides: "Custom 404 error page"
      min_lines: 10
    - path: "src/components/YouTubeEmbed.astro"
      provides: "Reusable YouTube iframe embed component"
      min_lines: 10
  key_links:
    - from: "src/pages/category/[slug].astro"
      to: "blog collection"
      via: "getStaticPaths filtering posts by category"
      pattern: "getCollection.*blog.*categories"
    - from: "src/pages/videos/[slug].astro"
      to: "videos collection"
      via: "getStaticPaths from videos collection"
      pattern: "getCollection.*videos"
---

<objective>
Create category archive pages, video pages (hub + individual), and a custom 404 page.

Purpose: These are the remaining page types needed for Phase 3 -- category archives (BLOG-03), video URLs (URL-05), and 404 (BLOG-07). Together with Plans 01-02, they complete all routing and core pages.
Output: Category archives, videos hub, video detail pages, YouTube embed component, 404 page.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-routing-and-core-pages/03-CONTEXT.md
@.planning/phases/03-routing-and-core-pages/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be completed first) -->

From src/utils/post-utils.ts:
```typescript
export function slugify(text: string): string;
export function sortPostsByDateDesc(postA, postB): number;
export function getReadingTime(content: string): number;
```

From src/content.config.ts (videos schema -- created in Phase 1, populated in Phase 2):
```typescript
// Videos collection uses glob loader from src/content/videos/
// Fields: title, youtubeId, duration, publishDate, excerpt, etc.
```

From src/layouts/BaseLayout.astro:
```typescript
// Props: title, description, image, pageType
// Provides full page shell with head, nav, footer
```

From src/components/PostPreview.astro:
```typescript
// Props: { post: CollectionEntry<'blog'> }
// Shows post card with date, categories, reading time, image, excerpt
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create category archive pages</name>
  <files>src/pages/category/[slug].astro</files>
  <action>
Create `src/pages/category/[slug].astro` that generates one page per unique category.

In `getStaticPaths()`:
- Fetch all blog posts via `getCollection('blog')`
- Build a `Map<string, CollectionEntry<'blog'>[]>` mapping category slugs to posts
- For each post, iterate its `categories` array. For each category, `slugify()` it and add the post to that slug's array
- Return entries as `{ params: { slug }, props: { categoryName, posts } }` where `categoryName` is the original display string (e.g., "Search Opinion") and `posts` are sorted by date descending

Template:
- Use BaseLayout with title `"{categoryName} - joost.blog"` and description `"Posts in the {categoryName} category"`
- Render a visible h1 with the category name
- Below: render ALL posts for that category using the PostPreview component (same card style as blog listing, per CONTEXT.md decision)
- No pagination (all posts on one page, per CONTEXT.md decision)

Import `slugify` and `sortPostsByDateDesc` from `../../utils/post-utils`.
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/joost.blog && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>Category archive pages exist for all categories (wordpress, development, search-opinion, yoast, open-source, etc.). Each shows all posts in that category.</done>
</task>

<task type="auto">
  <name>Task 2: Create video pages, YouTube embed component, and 404 page</name>
  <files>src/components/YouTubeEmbed.astro, src/pages/videos/index.astro, src/pages/videos/[slug].astro, src/pages/404.astro</files>
  <action>
**YouTubeEmbed component** (`src/components/YouTubeEmbed.astro`):
- Props: `youtubeId: string`, `title: string`
- Render a responsive `aspect-video` div containing an iframe
- Use `https://www.youtube-nocookie.com/embed/{youtubeId}` as src (privacy-enhanced mode)
- Add `loading="lazy"`, `allowfullscreen`, proper allow attributes
- Style with `w-full h-full rounded-lg`
- This is a simple iframe embed; Phase 4 will add facade pattern optimization

**Videos hub** (`src/pages/videos/index.astro`):
- Auto-generated from the videos collection (not static markdown)
- Fetch all videos via `getCollection('videos')`
- Sort by publishDate descending
- Use BaseLayout with title "Videos - joost.blog"
- Render a visible h1 "Videos"
- Display each video as a card: title (linked to `/videos/${video.id}/`), YouTube thumbnail image (`https://img.youtube.com/vi/${video.data.youtubeId}/maxresdefault.jpg`), duration if available, and excerpt/description
- Use a grid layout (1 col mobile, 2 cols md, 3 cols lg) for the video cards

**Individual video pages** (`src/pages/videos/[slug].astro`):
- In `getStaticPaths()`: fetch videos collection, map to `{ params: { slug: video.id }, props: { video } }`
- Template: BaseLayout with video title, render YouTubeEmbed component with the video's youtubeId, then render the video's markdown content below via `render(video)`
- Include duration display if available

**404 page** (`src/pages/404.astro`):
- Use BaseLayout with title "Page Not Found"
- Clean, minimal design: large "404" heading, friendly message ("The page you are looking for does not exist or has been moved."), prominent "Go Home" button linking to /
- Optionally show 3 recent blog posts below to help visitors find content
- Style consistently with the site's aesthetic (slate colors, primary accent for the button)
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/joost.blog && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>Videos hub at /videos/ lists all 8 videos. Individual video pages at /videos/{slug}/ show YouTube embed. 404 page displays at /404.html. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx astro build` succeeds with zero errors
- dist/category/wordpress/index.html exists
- dist/category/development/index.html exists
- dist/videos/index.html exists (auto-generated hub, not from videos.md)
- dist/videos/ contains subdirectories for each video slug
- dist/404.html exists
- Category pages contain PostPreview-style cards
- Video pages contain YouTube iframe embeds
</verification>

<success_criteria>
- All unique categories have archive pages at /category/{slug}/
- Videos hub auto-generates from collection data
- 8 individual video pages render YouTube embeds
- Custom 404 page exists with helpful content
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-routing-and-core-pages/03-03-SUMMARY.md`
</output>
