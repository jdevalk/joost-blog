---
phase: 03-routing-and-core-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/[slug].astro
  - src/pages/[...id].astro
  - src/pages/blog/[id].astro
  - src/utils/post-utils.ts
  - src/data/site-config.ts
  - src/pages/rss.xml.js
  - src/components/PostPreview.astro
  - src/components/FeaturedPostPreview.astro
  - src/components/ReadNextPostPreview.astro
  - src/content/pages/videos.md
autonomous: true
requirements:
  - URL-01
  - URL-02
  - URL-03
  - URL-04
  - URL-05
  - BLOG-05
  - BLOG-06
  - BLOG-04

must_haves:
  truths:
    - "Every blog post is accessible at /{slug}/ (root level, not /blog/)"
    - "Every static page is accessible at its original URL (/{slug}/)"
    - "All internal links point to /{slug}/ not /blog/{slug}/"
    - "RSS feed links point to root-level post URLs"
    - "postsPerPage is 10"
  artifacts:
    - path: "src/pages/[slug].astro"
      provides: "Merged route serving both blog posts and pages at root level"
      exports: ["getStaticPaths"]
    - path: "src/utils/post-utils.ts"
      provides: "Utilities: sortPostsByDateDesc, slugify, getReadingTime, getRelatedPosts"
      exports: ["sortPostsByDateDesc", "slugify", "getReadingTime", "getRelatedPosts"]
  key_links:
    - from: "src/pages/[slug].astro"
      to: "blog + pages collections"
      via: "getStaticPaths merging both collections"
      pattern: "getCollection.*blog.*getCollection.*pages"
    - from: "src/components/PostPreview.astro"
      to: "root-level URLs"
      via: "href using post.id without /blog/ prefix"
      pattern: "href=.*/${post.id}/"
---

<objective>
Rewire all routing from /blog/{slug}/ to /{slug}/ and build the utility functions that all subsequent plans depend on.

Purpose: This is the foundation -- every other plan in Phase 3 depends on correct root-level routing and shared utilities (slugify, getReadingTime, getRelatedPosts).
Output: Merged [slug].astro route, updated link references in all components, utility functions, RSS feed fix, postsPerPage change.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-routing-and-core-pages/03-CONTEXT.md
@.planning/phases/03-routing-and-core-pages/03-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/utils/post-utils.ts:
```typescript
import { type CollectionEntry } from 'astro:content';
export function sortPostsByDateDesc(postA: CollectionEntry<'blog'>, postB: CollectionEntry<'blog'>)
```

From src/data/site-config.ts:
```typescript
const siteConfig: SiteConfig = {
    // ...
    postsPerPage: 5  // MUST change to 10
};
```

From src/pages/[...id].astro (current pages route -- will be DELETED):
```typescript
export async function getStaticPaths() {
    const pages = await getCollection('pages');
    return pages.map((page) => ({
        params: { id: page.id },
        props: { page }
    }));
}
```

From src/pages/blog/[id].astro (current blog post route -- will be DELETED):
```typescript
export async function getStaticPaths() {
    const posts = (await getCollection('blog')).sort(sortPostsByDateDesc);
    return posts.map((post, index) => ({
        params: { id: post.id },
        props: { post, prevPost, nextPost }
    }));
}
```

Components with hardcoded /blog/ links that MUST be updated:
- src/components/PostPreview.astro: lines 33, 38 (`/blog/${post.id}/`)
- src/components/FeaturedPostPreview.astro: lines 27, 32, 55 (`/blog/${post.id}/`)
- src/components/ReadNextPostPreview.astro: lines 25, 32 (`/blog/${post.id}/`)
- src/pages/rss.xml.js: line 15 (`/blog/${item.id}/`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add utility functions and update site config</name>
  <files>src/utils/post-utils.ts, src/data/site-config.ts</files>
  <action>
In `src/utils/post-utils.ts`, add three new exported functions (keep existing `sortPostsByDateDesc`):

1. `slugify(text: string): string` -- lowercase, replace spaces with hyphens, strip non-word chars except hyphens, collapse double hyphens, trim. Used for category slugs.

2. `getReadingTime(content: string): number` -- split on whitespace, count words, divide by 225, ceil, min 1. Takes raw markdown body string.

3. `getRelatedPosts(currentPost, allPosts, limit=3): CollectionEntry<'blog'>[]` -- algorithm:
   - Filter out current post
   - Get posts sharing first category, sorted by date desc
   - If < limit, fill from other shared categories
   - If still < limit, fill with most recent posts from any category
   - Return up to `limit` posts

All functions should use proper TypeScript types with `CollectionEntry<'blog'>` from `astro:content`.

In `src/data/site-config.ts`, change `postsPerPage: 5` to `postsPerPage: 10`.
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/joost.blog && npx astro check 2>&1 | tail -20</automated>
  </verify>
  <done>post-utils.ts exports slugify, getReadingTime, getRelatedPosts alongside existing sortPostsByDateDesc. postsPerPage is 10.</done>
</task>

<task type="auto">
  <name>Task 2: Create merged root-level route and update all link references</name>
  <files>src/pages/[slug].astro, src/pages/[...id].astro, src/pages/blog/[id].astro, src/pages/rss.xml.js, src/components/PostPreview.astro, src/components/FeaturedPostPreview.astro, src/components/ReadNextPostPreview.astro, src/content/pages/videos.md</files>
  <action>
**Step 1: Handle the videos.md conflict.** The file `src/content/pages/videos.md` would conflict with the new `src/pages/videos/index.astro` (Plan 03). Either rename it to `videos-legacy.md` or delete it -- per CONTEXT.md, the videos hub should be auto-generated from the videos collection. Rename to `_videos.md` (prefixing with underscore excludes it from collection) to preserve the content but prevent routing conflict.

**Step 2: Create `src/pages/[slug].astro`** -- a merged dynamic route that serves BOTH blog posts and pages at root level. In `getStaticPaths()`:
- Fetch both `blog` and `pages` collections
- For blog posts: map to `{ params: { slug: post.id }, props: { entry: post, type: 'blog' as const } }`
- For pages: map to `{ params: { slug: page.id }, props: { entry: page, type: 'page' as const } }` -- but EXCLUDE any page whose id is 'videos' (handled by /videos/ route in Plan 03)
- Merge both arrays and return

In the template section, branch on `type`:
- For `type === 'blog'`: render the blog post template (copy from current `blog/[id].astro` but import from correct relative paths -- one level up, not two). Include reading time display (import `getReadingTime` from utils, compute from `entry.body`). Add category links (each links to `/category/{slugify(cat)}/`). Replace the prev/next ReadNextPostPreview section with a RelatedPosts section (import `getRelatedPosts`, compute in frontmatter, render up to 3 related posts as simple cards with title, date, and link to `/${post.id}/`).
- For `type === 'page'`: render the page template (copy from current `[...id].astro` but adjust relative imports)

IMPORTANT for blog post template: Add reading time next to date in the header metadata line. Add category links (comma-separated, each linking to `/category/{slugify(cat)}/`) in the header metadata. Keep the share links, subscribe component, and feature image handling from the current blog/[id].astro.

For related posts in the blog template: render a "Related Posts" section (replacing "Read Next") showing up to 3 posts. Each related post card should show: title (linked to `/${post.id}/`), date, and optional feature image thumbnail. Style consistently with the existing ReadNextPostPreview aesthetic.

**Step 3: Delete old route files:**
- Delete `src/pages/blog/[id].astro` (replaced by merged route)
- Delete `src/pages/[...id].astro` (replaced by merged route)

**Step 4: Update all `/blog/` link references:**
- `src/components/PostPreview.astro`: change `/blog/${post.id}/` to `/${post.id}/` (2 occurrences, lines 33 and 38)
- `src/components/FeaturedPostPreview.astro`: change `/blog/${post.id}/` to `/${post.id}/` (3 occurrences, lines 27, 32, 55)
- `src/components/ReadNextPostPreview.astro`: change `/blog/${post.id}/` to `/${post.id}/` (2 occurrences) -- this component is being replaced by inline related posts in [slug].astro, but update it anyway in case it's referenced elsewhere
- `src/pages/rss.xml.js`: change link from `/blog/${item.id}/` to `/${item.id}/`

**Step 5: Verify no remaining `/blog/` hardcoded references** by searching the codebase. The `/blog` nav link in site-config.ts and the blog listing page route (`src/pages/blog/[...page].astro`) should REMAIN -- those are for the blog listing, not individual post URLs.
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/joost.blog && npx astro build 2>&1 | tail -30</automated>
  </verify>
  <done>All blog posts accessible at /{slug}/, all pages at their original URLs, no /blog/{slug}/ links remain in components, RSS links point to root-level URLs, videos.md conflict resolved. Build succeeds with zero errors.</done>
</task>

</tasks>

<verification>
- `npx astro build` completes with zero errors
- Spot check: dist/agent-ready-plugins/index.html exists (blog post at root)
- Spot check: dist/about-me/index.html exists (page at root)
- Spot check: dist/blog/index.html exists (blog listing still works)
- No files in dist/blog/ except pagination pages (index.html, 2/index.html, etc.)
- grep -r '/blog/' dist/ should only find blog listing pagination links, not post links
</verification>

<success_criteria>
- Every blog post builds at /{slug}/index.html in dist/
- Every page builds at /{slug}/index.html in dist/
- Blog listing builds at /blog/index.html with pagination
- RSS feed references /{slug}/ URLs
- postsPerPage is 10
- Utility functions (slugify, getReadingTime, getRelatedPosts) available for Plan 02 and 03
</success_criteria>

<output>
After completion, create `.planning/phases/03-routing-and-core-pages/03-01-SUMMARY.md`
</output>
