---
phase: 03-routing-and-core-pages
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/PostPreview.astro
  - src/pages/blog/[...page].astro
autonomous: true
requirements:
  - BLOG-01
  - BLOG-02

must_haves:
  truths:
    - "Blog listing page at /blog/ shows posts with date, category links, and reading time"
    - "Blog listing paginates at 10 posts per page"
    - "Category names in post cards link to /category/{slug}/"
    - "Blog listing page title says 'Blog' not 'Ovidius Blog'"
  artifacts:
    - path: "src/components/PostPreview.astro"
      provides: "Post card with date, category links, and reading time"
      min_lines: 40
    - path: "src/pages/blog/[...page].astro"
      provides: "Blog listing with updated title"
      min_lines: 30
  key_links:
    - from: "src/components/PostPreview.astro"
      to: "/category/{slug}/"
      via: "anchor tags using slugify utility"
      pattern: "slugify.*category"
    - from: "src/components/PostPreview.astro"
      to: "src/utils/post-utils.ts"
      via: "import getReadingTime, slugify"
      pattern: "import.*getReadingTime.*post-utils"
---

<objective>
Enhance the blog listing page and PostPreview component with category links and reading time display.

Purpose: BLOG-01 (listing with pagination) and BLOG-02 (date, category, reading time on posts) are core blog features users expect.
Output: Enhanced PostPreview showing categories and reading time, updated blog listing page title.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-routing-and-core-pages/03-CONTEXT.md
@.planning/phases/03-routing-and-core-pages/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be completed first) -->

From src/utils/post-utils.ts (created in Plan 01):
```typescript
export function slugify(text: string): string;
export function getReadingTime(content: string): number;
export function getRelatedPosts(currentPost, allPosts, limit?): CollectionEntry<'blog'>[];
export function sortPostsByDateDesc(postA, postB): number;
```

From src/components/PostPreview.astro (current state after Plan 01 link fixes):
```astro
---
import { type CollectionEntry } from 'astro:content';
const { post } = Astro.props as { post: CollectionEntry<'blog'> };
const { title, publishDate, updatedDate, featureImage, excerpt, isFeatured } = post.data;
---
<!-- Links already updated to /${post.id}/ by Plan 01 -->
```

From src/data/site-config.ts (updated in Plan 01):
```typescript
postsPerPage: 10
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance PostPreview with categories and reading time</name>
  <files>src/components/PostPreview.astro</files>
  <action>
Enhance the existing PostPreview component to display category links and reading time alongside the existing date.

In the frontmatter:
- Import `slugify` and `getReadingTime` from `../utils/post-utils`
- Import `render` from `astro:content`
- Extract `categories` from `post.data`
- Call `render(post)` and use the rendered body or `post.body` to compute reading time via `getReadingTime(post.body)`

In the template, update the metadata line (the div with `text-sm tracking-wider uppercase`) to show:
1. Featured badge (keep existing)
2. Date (keep existing FormattedDate)
3. Reading time: add ` / {readingTime} min read` after date
4. Categories: add category links after reading time. Each category as `<a href="/category/{slugify(cat)}/" class="text-primary hover:text-slate-700 dark:text-accent dark:hover:text-slate-300 transition duration-300">{cat}</a>`, separated by commas. Wrap in a span or div that sits on the same metadata line.

Style the categories to be visually distinct but harmonious -- use the primary/accent color for links (consistent with existing link styling in the theme). Keep the uppercase tracking-wider style for consistency with the metadata line.

Keep ALL existing functionality (feature image, excerpt, grid layout on xl, etc.) unchanged.
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/joost.blog && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>PostPreview shows date, reading time, and clickable category links for every blog post card.</done>
</task>

<task type="auto">
  <name>Task 2: Update blog listing page title and metadata</name>
  <files>src/pages/blog/[...page].astro</files>
  <action>
Update the blog listing page:

1. Change the BaseLayout title from "Ovidius Blog" to "Blog - joost.blog" (or just "Blog" and let BaseLayout append site name if it does that automatically -- check BaseLayout behavior)
2. Change the description from "Get all the latest news from Ovidius" to something appropriate like "Articles about WordPress, SEO, open source, and more by Joost de Valk"
3. Change the sr-only h1 from "Blog" to a visible h1 -- make it a proper page heading: `<h1 class="text-3xl font-bold text-slate-900 dark:text-slate-100 mb-8 sm:text-4xl">Blog</h1>` placed before the post list
4. Keep all pagination, Subscribe component, and PostPreview usage exactly as-is
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/joost.blog && npx astro build 2>&1 | tail -10</automated>
  </verify>
  <done>Blog listing page has proper title, description, and visible heading. Pagination works with 10 posts per page.</done>
</task>

</tasks>

<verification>
- `npx astro build` succeeds
- dist/blog/index.html contains category links and reading time in post cards
- dist/blog/index.html title is not "Ovidius Blog"
- Blog listing paginates correctly (75 posts / 10 per page = 8 pages)
</verification>

<success_criteria>
- PostPreview displays date, categories (as links), and reading time
- Blog listing has proper title and visible heading
- Pagination works with 10 posts per page
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-routing-and-core-pages/03-02-SUMMARY.md`
</output>
