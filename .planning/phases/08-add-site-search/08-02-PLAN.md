---
phase: 08-add-site-search
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/pages/search.astro
  - src/components/Header.astro
autonomous: true
requirements: [SEARCH-04, SEARCH-05]

must_haves:
  truths:
    - "User can navigate to /search/ and perform a full-text search"
    - "Search results link to correct post/page URLs"
    - "Search UI matches site theme in both light and dark mode"
    - "Search icon is visible in header on both mobile and desktop"
  artifacts:
    - path: "src/pages/search.astro"
      provides: "Dedicated search page with Pagefind UI"
      min_lines: 20
    - path: "src/components/Header.astro"
      provides: "Search icon trigger linking to /search/"
      contains: "search"
  key_links:
    - from: "src/components/Header.astro"
      to: "/search/"
      via: "anchor link with search icon"
      pattern: "href=\"/search/\""
    - from: "src/pages/search.astro"
      to: "astro-pagefind"
      via: "Search component import"
      pattern: "astro-pagefind/components/Search"
---

<objective>
Create the search page with Pagefind UI, add a search icon to the header, and style the search UI for dark mode.

Purpose: Users need a discoverable way to find content across ~80 pages. The search page uses Pagefind's built-in UI which handles debouncing, highlighting, excerpts, and accessibility.
Output: Working search at /search/ with themed UI and header trigger.
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-add-site-search/08-RESEARCH.md
@.planning/phases/08-add-site-search/08-01-SUMMARY.md
@src/components/Header.astro
@src/layouts/BaseLayout.astro
</context>

<interfaces>
<!-- From src/layouts/BaseLayout.astro -->
BaseLayout accepts props: title, description, image, pageType
Usage: <BaseLayout title="Search" description="...">

<!-- From src/components/Header.astro -->
Header has desktop nav links (hidden md:flex) and a ThemeToggle.
The search icon should go between the desktop nav and ThemeToggle in the flex container.
On mobile, it should also be visible (not inside hamburger menu).

<!-- From astro-pagefind (per research) -->
import Search from 'astro-pagefind/components/Search';
<Search id="search" className="pagefind-ui" uiOptions={{...}} />
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create search page and add header search icon</name>
  <files>src/pages/search.astro, src/components/Header.astro</files>
  <action>
    **Create `src/pages/search.astro`:**
    - Import BaseLayout and Search from 'astro-pagefind/components/Search'
    - Use BaseLayout with title="Search" and description="Search all posts and pages on joost.blog"
    - Wrap the Search component in `<div data-pagefind-ignore="all">` so the search page itself is not indexed
    - Do NOT add `data-pagefind-body` to this page's main
    - Use uiOptions: `{ showSubResults: true, showImages: true, pageSize: 10, resetStyles: true }`
    - Add `<style is:global>` block with Pagefind CSS variable overrides for both light and dark mode:
      - Light: primary=#3b82f6, text=#334155, background=#ffffff, border=#e2e8f0, tag=#f1f5f9
      - Dark (.dark): primary=#60a5fa, text=#e2e8f0, background=#1e293b, border=#334155, tag=#334155
      - Also set: --pagefind-ui-scale: 1, --pagefind-ui-border-width: 1px, --pagefind-ui-border-radius: 8px, --pagefind-ui-image-border-radius: 8px, --pagefind-ui-image-box-ratio: 3 / 2, --pagefind-ui-font: inherit
    - Layout: `<main class="max-w-3xl mx-auto px-4 py-8 sm:py-12 grow">`
    - Page heading: `<h1 class="text-3xl font-bold mb-8 dark:text-slate-100">Search</h1>`

    **Modify `src/components/Header.astro`:**
    - Add a search icon link BETWEEN the desktop nav and ThemeToggle (so it appears next to the theme toggle on all screen sizes).
    - Place it right before `<ThemeToggle />` in the flex container (line 41 area).
    - Use this markup:
      ```
      <a href="/search/"
        class="inline-flex items-center justify-center w-8 h-8 text-slate-600 hover:text-primary dark:text-slate-300 dark:hover:text-accent transition-colors"
        aria-label="Search">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24"
          fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="11" cy="11" r="8" />
          <path d="m21 21-4.3-4.3" />
        </svg>
      </a>
      ```
    - The search icon must be visible on ALL screen sizes (not hidden behind hamburger menu). Place it in the `<div class="flex items-center gap-4">` container alongside ThemeToggle.
  </action>
  <verify>
    <automated>cd /Users/joostdevalk/Code/joost.blog && test -f src/pages/search.astro && grep -q "astro-pagefind" src/pages/search.astro && grep -q "/search/" src/components/Header.astro && npm run build 2>&1 | tail -5 && test -d dist/search && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Search page exists at /search/ with Pagefind UI, dark mode styling, and a search icon in the header visible on all screen sizes. Build succeeds and dist/search/ directory is created.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `dist/search/index.html` exists
- Search page imports astro-pagefind Search component
- Header has search icon linking to /search/
- Pagefind CSS variables set for both light and dark mode
- Search page itself is not indexed (data-pagefind-ignore)
</verification>

<success_criteria>
Users can click search icon in header, navigate to /search/, type a query, and see relevant results from blog posts and pages. The search UI matches the site's light/dark theme.
</success_criteria>

<output>
After completion, create `.planning/phases/08-add-site-search/08-02-SUMMARY.md`
</output>
